export default function App() {
  return (
    <main className="dev-shell" aria-label="Convergence core shell">
      <h1>Convergence Core</h1>
      <p>Current UI removed. Core services are ready for the next interface.</p>
    </main>
  );
}
